<div class="card">
  <% if prototype.image.attached? %>
    <%= link_to image_tag(prototype.image, style: 'max-width: 200px;'), root_path %>
  <% end %>
  <h3><%= link_to prototype.title, root_path %></h3>
  <p><%= prototype.catch_copy %></p>
  <p>投稿者: <%= prototype.user.name %></p>
</div> 
